const config = {
    darkMode: 'class',
    content: ['./src/**/*.{html,js,svelte,ts}'],
    theme: {
        extend: {
            colors: {
                mainDark: '#1e3851',
            },
        },
    },
    plugins: [],
};

module.exports = config;
